% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import.R
\name{read_export}
\alias{read_export}
\title{Read a single Sleep Diaries JSON export}
\usage{
read_export(path, participant_id = NULL, compute_vars = TRUE)
}
\arguments{
\item{path}{Path to a \code{.json} file exported from the Sleep Diaries app.}

\item{participant_id}{Optional character string used as the \code{participant_id}
column value. If \code{NULL} (default), uses the \code{researchCode} field from the
JSON, falling back to the \code{participant} name field, then to the filename.}

\item{compute_vars}{Logical. If \code{TRUE} (default), adds derived sleep
variables (TIB, TST, SE, flags) to morning entries via
\code{\link[=compute_sleep_vars]{compute_sleep_vars()}}.}
}
\value{
A named list with class \code{"slumbr_export"}:
\describe{
\item{\code{participant_id}}{Character. The resolved participant identifier.}
\item{\code{participant_name}}{Character. Raw name from JSON.}
\item{\code{research_code}}{Character or \code{NA}. Research code from JSON.}
\item{\code{exported_at}}{POSIXct. Export timestamp.}
\item{\code{diary}}{Data frame. One row per diary entry (morning or evening),
with all parsed answer columns.}
\item{\code{questionnaires}}{Data frame. One row per completed questionnaire,
with raw answers in a list-column and computed scores.}
\item{\code{raw}}{List. The full parsed JSON, for debugging.}
}
}
\description{
Parses one participant's JSON export file as produced by the Sleep Diaries
app and returns a structured list with raw entries, parsed diary data, and
questionnaire results.
}
\examples{
\dontrun{
p <- read_export("exports/P001.json")
p$diary
p$questionnaires
}

}
\seealso{
\code{\link[=read_study]{read_study()}}, \code{\link[=diary_long]{diary_long()}}, \code{\link[=diary_wide]{diary_wide()}}
}
