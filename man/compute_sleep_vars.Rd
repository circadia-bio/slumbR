% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import.R
\name{compute_sleep_vars}
\alias{compute_sleep_vars}
\title{Compute derived sleep variables from raw morning diary answers}
\usage{
compute_sleep_vars(df)
}
\arguments{
\item{df}{A data frame with morning-entry columns (output of \code{\link[=diary_long]{diary_long()}}).}
}
\value{
The same data frame with additional computed columns appended.
}
\description{
Given a morning-entry data frame row (as produced internally by
\code{\link[=read_export]{read_export()}}), returns additional computed columns:
}
\details{
\tabular{ll}{
   Variable \tab Definition \cr
   \code{tib_min} \tab Time in bed (rise_time − bed_time), minutes \cr
   \code{tst_min} \tab Total sleep time (TIB − SOL − WASO), minutes \cr
   \code{se_pct} \tab Sleep efficiency (TST / TIB × 100), \% \cr
   \code{sol_flag} \tab \code{TRUE} if SOL > 30 min (clinically significant) \cr
   \code{se_flag} \tab \code{TRUE} if SE < 85\% (below healthy threshold) \cr
   \code{waso_flag} \tab \code{TRUE} if WASO > 30 min (clinically significant) \cr
   \code{tst_flag} \tab \code{TRUE} if TST < 7 h (below recommended) \cr
}


Clock-time fields (bed_time, rise_time) are decimal hours. Overnight wrap
is handled by adding 24 when rise_time ≤ bed_time.
}
