% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import.R
\name{read_study}
\alias{read_study}
\title{Read a directory of Sleep Diaries JSON exports into a study object}
\usage{
read_study(
  dir,
  pattern = "\\\\.json$",
  participant_id_from = c("code", "name", "filename"),
  compute_vars = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{dir}{Path to a directory containing \code{.json} export files.}

\item{pattern}{Regex pattern to match filenames. Default \code{"\\\\.json$"}.}

\item{participant_id_from}{One of \code{"code"} (use \code{researchCode} field,
fallback to name then filename), \code{"name"} (use the \code{participant} name
field), or \code{"filename"} (always use the filename without extension).}

\item{compute_vars}{Logical. Passed to \code{\link[=read_export]{read_export()}}.}

\item{verbose}{Logical. Print a progress summary. Default \code{TRUE}.}
}
\value{
A named list with class \code{"slumbr_study"}:
\describe{
\item{\code{diary}}{Long-format data frame. All participants, all entries.}
\item{\code{wide}}{Wide-format data frame. One row per participant-night,
morning and evening columns merged side by side.}
\item{\code{scores}}{Data frame. Questionnaire scores, one row per
participant × instrument.}
\item{\code{participants}}{Character vector of resolved participant IDs.}
\item{\code{exports}}{Named list of individual \code{slumbr_export} objects.}
}
}
\description{
Reads all \code{.json} files in a directory (one per participant) and assembles
them into a \code{slumbr_study} object containing combined long and wide data
frames plus questionnaire scores.
}
\examples{
\dontrun{
study <- read_study("exports/")
study$diary
study$wide
study$scores
}

}
\seealso{
\code{\link[=read_export]{read_export()}}, \code{\link[=diary_long]{diary_long()}}, \code{\link[=diary_wide]{diary_wide()}}
}
